version: '3.3'
services:
    heroku-gone:
        ports:
            - '5012:5012'
        restart: always
        image: heroku-gone
        build:
            context: .
            dockerfile: Dockerfile
