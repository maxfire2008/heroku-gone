version: '3.3'
services:
    heroku-gone:
        ports:
            - '3000:3012'
        restart: always
        image: heroku-gone
        build:
            context: .
            dockerfile: Dockerfile
